#include "prevariety_types.h"

//------------------------------------------------------------------------------
bool ConeTreeSortFunction (ConeWithIndicator i, ConeWithIndicator j)
{
   return (i.RayIndices < j.RayIndices);
}

//------------------------------------------------------------------------------
void SortConeTree(void)
{
//   for (size_t i = 0; i != ConeTree.size(); i++)
   {
  //    sort(ConeTree[i].begin(), ConeTree[i].end(), ConeTreeSortFunction);
   };
}
